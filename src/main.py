from handler import Handler
def main():
    s = Handler()
    while True:
        pass
#create communication between users: one person creates a chat link. creates chat in server with unique identifier (URL)
#anyone with the identifier can join the chat. chat automatically deletes within one hour

main()
    